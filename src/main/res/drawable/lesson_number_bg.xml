<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/lessonNumberBg" />
    <corners android:radius="18dp" />
    <!-- Обводка только для темных тем (светлая тема использует тот же цвет что и фон) -->
    <stroke
        android:width="1dp"
        android:color="?attr/lessonNumberStroke" />
</shape>
